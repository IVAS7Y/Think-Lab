[project]
name = "think-lab"
version = "1.0.0"
description = "SRLN instrument-data staging and Oak transfer controller"
requires-python = ">=3.11"

[project.scripts]
think-lab = "think_lab:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
